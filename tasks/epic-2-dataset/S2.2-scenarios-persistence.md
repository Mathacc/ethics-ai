# S2.2 — Scenarios + persistence

- **Epic:** 2 — Synthetic dataset generation
- **Story points:** 3
- **Status:** ☑ Done
- **Depends on:** S2.1

## Goal
Preset scenarios that stress fairness, plus dataset save/load.

## Tasks
- [ ] Preset: `balanced` (even skills/diversity)
- [ ] Preset: `skewed-skill` (a few high-skill participants)
- [ ] Preset: `minority-underrepresented` (one diversity group is rare)
- [ ] `save(participants, path)` / `load(path)` for CSV + JSON
- [ ] CLI hook: `generate --scenario <name>`

## Acceptance criteria
- Each scenario produces visibly different distributions.
- CSV and JSON round-trip without data loss.
- Scenarios are reproducible with a seed.
