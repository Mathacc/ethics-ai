# S1.1 — Repo scaffold + tooling

- **Epic:** 1 — Project setup & data model
- **Story points:** 2
- **Status:** ☑ Done
- **Depends on:** —

## Goal
Set up the Python package, dependencies, and tests so the team can start coding from day one.

## Tasks
- [x] Pin Python via `.python-version` (3.11.9, pyenv)
- [x] `requirements.txt` with deps: `numpy`, `pandas`, `matplotlib`, `ortools`, `pyyaml`, `pytest`
- [x] Package layout `groupformation/` + `tests/` + `data/` + `outputs/`
- [x] `.gitignore` (keep `data/`/`outputs/` via `.gitkeep`)
- [x] README with setup + run instructions
- [x] Smoke tests (`tests/test_smoke.py`)

## Acceptance criteria
- venv on 3.11.9 installs `requirements.txt` cleanly. ✅
- `pytest` runs and passes the smoke tests. ✅
- `python -m groupformation.cli --help` works. ✅

## Notes
- No `pyproject.toml` by request — kept simple with `requirements.txt`.
- Models use stdlib `dataclasses` (no pydantic).
- Run from repo root so `groupformation` is importable, or activate the venv.
