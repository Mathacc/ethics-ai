# S4.2 — Weighted cost function

- **Epic:** 4 — Algorithms
- **Story points:** 3
- **Status:** ☑ Done
- **Depends on:** S3.2, S1.3

## Goal
Aggregate soft-constraint terms into a single scalar the optimizers minimize.

## Tasks
- [ ] `cost(assignment, config) = Σ wᵢ · termᵢ(assignment)`
- [ ] Read weights from `Config`
- [ ] Provide an incremental/delta cost for a single swap (perf for local search)
- [ ] Sanity test: delta cost == full recompute difference

## Acceptance criteria
- Single source of truth for "how good" an assignment is.
- Delta evaluation matches full recompute (property test).
- Weight changes shift the optimum as expected.
